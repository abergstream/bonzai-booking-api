# "org" ensures this Service is used with the correct Serverless Framework Access Key.
org:
# "service" is the name of this project. This will also be added to your AWS resource names.
service: bonzai

provider:
  name: aws
  runtime: nodejs20.x
  region: eu-north-1
  iam:
    role:
      statements:
        - Effect: "Allow"
          Action:
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource: "*"
build:
  esbuild:
    minify: true

package:
  individually: true

functions:
  overview:
    handler: functions/bookingOverview/handler.getOverview
    events:
      - httpApi:
          path: /overview
          method: get
  availableRooms:
    handler: functions/bookRoom/handler.availableRooms
    events:
      - httpApi:
          path: /rooms
          method: get
  bookRoom:
    handler: functions/bookRoom/handler.bookRoom
    events:
      - httpApi:
          path: /room
          method: post
  deleteRoom:
    handler: functions/deleteBooking/handler.deleteBooking
    events:
      - httpApi:
          path: /room
          method: delete
  updateBooking:
    handler: functions/bookRoom/handler.updateBooking
    events:
      - httpApi:
          path: /room
          method: put

resources:
  Resources:
    roomsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: bonzai_rooms
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST
    bookingsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: bonzai_bookings
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST
